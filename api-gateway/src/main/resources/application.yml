spring:
  application:
    name: api-gateway
  sleuth:
    sampler:
      baseUrl: http://localhost:9411
      probability: 1.0

server:
  port: 8765

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

